# Repository Guidelines

- Keep changes small and focused.
- Preserve the current CLI and `/tmp/site` output behavior unless asked otherwise.
- Prefer simple Bash and existing tools already used by `sitescrape`.
- Run `bash -n sitescrape` after shell changes.
- Update `README.md` if behavior or usage changes.
